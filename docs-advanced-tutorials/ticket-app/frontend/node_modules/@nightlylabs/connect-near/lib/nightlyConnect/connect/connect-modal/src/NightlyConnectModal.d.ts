import { NETWORK } from '../../../../common/types';
export declare class NightlyConnectModal {
    private _lastSessionId;
    private _styleElement;
    private _anchorElement;
    private _modalElement;
    private _network;
    onOpen: (() => void) | undefined;
    onClose: (() => void) | undefined;
    openModal(sessionId: string, network: NETWORK): void;
    closeModal(): void;
    copySessionId(): void;
}
