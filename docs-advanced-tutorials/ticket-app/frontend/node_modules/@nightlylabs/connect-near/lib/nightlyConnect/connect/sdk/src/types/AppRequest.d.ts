import { NETWORK } from '../../../../../common/types';
export declare enum AppRequest {
    SignTransaction = "SignTransaction",
    SignAllTransactions = "SignAllTransactions",
    SignMessage = "SignMessage"
}
export interface SignTransactionsRequest {
    type: AppRequest.SignTransaction | AppRequest.SignAllTransactions;
    transactions: string[];
    publicKey: string;
    network: NETWORK;
    id: string;
}
export interface SignMessageRequest {
    type: AppRequest.SignMessage;
    message: string;
    publicKey: string;
    network: NETWORK;
    id: string;
}
export declare type AppRequests = SignTransactionsRequest | SignMessageRequest;
