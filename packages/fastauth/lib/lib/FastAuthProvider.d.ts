import React from "react";
interface FastAuthProviderProps {
    children: React.ReactNode;
    clientId: string;
}
declare const FastAuthProvider: React.FC<FastAuthProviderProps>;
export default FastAuthProvider;
